<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TextEase.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TextEase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TextEase">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton x:Name="UndoButton" Icon="Undo" Label="Undo" Click="UndoButton_Click"/>
            <AppBarButton x:Name="RedoButton" Icon="Redo" Label="Redo" Click="RedoButton_Click"/>
            <AppBarSeparator/>
            <AppBarToggleButton x:Name="WordWrapButton" Icon="List" Label="Word Wrap" Click="WordWrapButton_Click"/>
        </CommandBar>

        <!-- Main Editor -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="MainEditor"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     PlaceholderText="Start typing..."
                     TextChanged="MainEditor_TextChanged"
                     SelectionChanged="MainEditor_SelectionChanged"
                     Margin="12"/>
        </ScrollViewer>

        <!-- Status Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="12,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="SelectionStats" Grid.Column="0" Text="Ln 1, Col 1"/>
            <TextBlock x:Name="WordCount" Grid.Column="2" Text="Words: 0"/>
        </Grid>
    </Grid>
</Window>
